name: Build
on:
  - pull_request
  - push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - uses: actions/setup-node@v3
        with:
          node-version: '22.x'
      - name: Run install
        run: npm ci
      - name: Run build
        run: npm run build
      - name: Replace path
        run: sed -i 's|/apps/developer/|${BASE_PATH}/developer/|g' dist/developer/browser/index.html
      - name: Archive production artifacts
        uses: actions/upload-artifact@v4
        with:
          name: developer
          include-hidden-files: true
          path: dist/developer/browser
